[package]
name = "tools"
version = "0.1.0"
edition = "2024"

[dependencies]
rig = "0.40.0"
rig-core = "0.40.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
